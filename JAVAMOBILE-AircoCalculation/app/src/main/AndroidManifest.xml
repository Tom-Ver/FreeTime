<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.vandenborre.aircocalculation">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_vanden_borre_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_vanden_borre_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AircoCalculation">
        <activity
            android:name=".CalculationActivity"
            android:exported="true"
            android:screenOrientation="portrait">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
    </application>

</manifest>